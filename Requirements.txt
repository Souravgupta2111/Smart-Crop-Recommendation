streamlit==1.33.0
pandas==2.2.2
numpy==1.26.4
matplotlib==3.9.0
seaborn==0.13.2
scikit-learn==1.5.0
statsmodels==0.14.2
# The following are typically built-in or implicitly handled, but good to list for clarity if used explicitly
# io (built-in)
# re (built-in)
# json (built-in)
# datetime (built-in)
# warnings (built-in)
